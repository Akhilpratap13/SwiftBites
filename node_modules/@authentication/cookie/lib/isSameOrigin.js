"use strict";

Object.defineProperty(exports, "__esModule", { value: true });
const url_1 = require("url");
function isCrossOrigin(header, req, baseURL) {
    if (!header) {
        return false;
    }
    const actual = new url_1.URL(header + '');
    // in production, it must match the explicitly supplied origin
    if (baseURL && actual.origin === baseURL.origin) {
        return false;
    }
    // in development or when no explicit baseURL is provided, it
    // can match either the host, or the x-forwarded-host
    if (process.env.NODE_ENV === 'development' || !baseURL) {
        if (actual.origin === actual.protocol + '//' + req.headers.host) {
            return false;
        }
        if (actual.origin === actual.protocol + '//' + req.headers['x-forwarded-host']) {
            return false;
        }
    }
    return true;
}
exports.isCrossOrigin = isCrossOrigin;
function isSameOrigin(req, baseURL) {
    return (req.headers.referer || req.headers.origin) && !isCrossOrigin(req.headers.referer, req, baseURL) && !isCrossOrigin(req.headers.origin, req, baseURL);
}
exports.default = isSameOrigin;
//# sourceMappingURL=isSameOrigin.js.map