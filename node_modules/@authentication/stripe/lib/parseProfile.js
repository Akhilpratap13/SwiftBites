"use strict";

Object.defineProperty(exports, "__esModule", { value: true });
function parseProfile(id, json) {
    const emails = [];
    if (json.email) {
        emails.push({ value: json.email, type: 'primary' });
    }
    if (json.support_email) {
        emails.push({ value: json.support_email, type: 'support' });
    }
    const images = [];
    if (json.business_logo) {
        images.push({
            url: json.business_logo
        });
    }
    return {
        provider: 'stripe',
        id,
        displayName: json.display_name,
        emails,
        images,
        image: images[0]
    };
}
exports.default = parseProfile;
//# sourceMappingURL=parseProfile.js.map