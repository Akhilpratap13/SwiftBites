/// <reference types="stripe" />
import Stripe = require('stripe');
import { Profile } from '@authentication/types';
export default function parseProfile(id: string, json: Stripe.accounts.IAccount): Profile;
